// const numbers = [1, -5, 666, 2, 400, 11];

// // 1. Sort the array of numbers from descending to ascending
// // order.

console.log(numbers.sort((a, b) => a - b));

// // 2. Sort the array of numbers from ascending to descending
// // order.
numbers.sort((a, b) => b - a);
console.log(numbers);

// my
// const descendinToAscending = nunmbers.sort((a,b)=>a-b)

// const acendingToDescendin = nunmbers.sort((a,b)=>b-a)
